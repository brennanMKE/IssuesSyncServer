# 0001 — Phase A: Backend skeleton

| | |
|---|---|
| **Status** | resolved |
| **Module** | db / storage / infra |
| **Platform** | All |
| **First seen** | 2026-05-18 |
| **Closed** | 2026-05-18 |
| **Commit** | c2067c4 |

## Description

Set up the foundational Go project structure for the `issued` binary. This is Phase A from `RemoteSync.md` and is the prerequisite for all subsequent phases.

Deliverables:

- Go module (`cmd/issued/main.go`) with packages `auth`, `api`, `ws`, `admin`, `storage`, `db`, `wire`
- Postgres migration runner and initial schema (all tables from `RemoteSync.md § Postgres schema`)
- S3 client wiring; IAM role on EC2 (no static credentials)
- `GET /healthz` returning `200` with build SHA
- Structured JSON logging to stdout via `slog`
- `systemd` unit file (`issued.service`): `Restart=on-failure`, `User=issued`, listens on `127.0.0.1:8080`
- Apache 2 VirtualHost config: `mod_proxy` + `mod_proxy_http` for HTTP, `mod_proxy_wstunnel` for `/v1/events`; `ProxyTimeout 3600`; TLS via existing certbot setup
- Environment variable loading (`BASE_URL`, `DATABASE_URL`, `S3_BUCKET`, `AWS_REGION`, etc.)
- Single static binary build, ~15 MB

## Notes

**S3 client**: use AWS SDK v2 (`github.com/aws/aws-sdk-go-v2/service/s3`). Configure the client from env at startup — `S3_ENDPOINT` + `S3_PATH_STYLE` cover all S3-compatible providers; omitting static credential vars falls back to IAM role auto-detection on AWS. The `s3/manager` sub-package handles multipart uploads (Phase J).

Apache 2 is already running on the EC2 box with certbot managing TLS. The `mod_proxy_wstunnel` `ProxyPass` directive for `/v1/events` must appear before the catch-all `ProxyPass /` in the VirtualHost block or Apache will treat WebSocket upgrades as plain HTTP.

Admin HTML/CSS/JS should be embedded via `embed.FS` so the binary is fully self-contained.

## Root cause

No Go code existed — this was a greenfield implementation task for the foundational backend skeleton.

## Fix

Created the full package layout under `cmd/issued/` and `internal/` with all required packages. The entry point loads env vars (fail-fast on missing required vars), initialises slog JSON logging, connects to Postgres via pgxpool, runs migrations, builds the S3 client, starts the HTTP server on `127.0.0.1:8080`, and shuts down gracefully on SIGTERM/SIGINT. The migration runner embeds SQL files via `//go:embed` and applies them transactionally against a `schema_migrations` tracking table. The full Postgres schema from `RemoteSync.md` landed in `001_initial.sql`. `deploy/issued.service` and `deploy/issued.conf` cover systemd and Apache 2 respectively.

## Files changed

- `go.mod` / `go.sum` — module `sync.sstools.co`, all deps (aws-sdk-go-v2, pgx/v5, go-webauthn/webauthn, golang-jwt/jwt/v5)
- `Makefile` — `build` / `test` / `clean`; injects build SHA via `-ldflags -X`
- `cmd/issued/main.go` — entry point, env loading, server wiring, graceful shutdown
- `cmd/issued/version.go` — `BuildSHA` var overridden at link time
- `internal/api/router.go` — `NewRouter`, stdlib `net/http` mux, stub routes
- `internal/api/healthz.go` — `GET /healthz` → `{"status":"ok","sha":"…"}`
- `internal/db/migrate.go` — migration runner with `schema_migrations` tracking
- `internal/db/migrations/001_initial.sql` — full schema (9 tables)
- `internal/storage/s3.go` — S3 client (static creds or IAM role, custom endpoint, path-style)
- `internal/storage/cache.go` — thread-safe LRU cache (~64 MB cap)
- `internal/wire/types.go` — JSON types matching `RemoteProtocol.swift`
- `internal/auth/auth.go` — Phase B stub
- `internal/ws/hub.go` — Phase E stub
- `internal/admin/admin.go` — Phase F stub
- `deploy/issued.service` — systemd unit
- `deploy/issued.conf` — Apache 2 VirtualHost (TLS 443, WS upgrade, HTTP→HTTPS redirect)
- `.gitignore` — anchored `/issued` so `cmd/issued/` directory is not ignored

## Gotchas

The `.gitignore` had `issued` (unanchored) which matched the `cmd/issued/` directory, silently excluding the two main entry-point files from `git add`. Changed to `/issued` to match only the root binary artifact.
